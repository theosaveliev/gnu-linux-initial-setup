---
- name: Copy daemon conf
  ansible.builtin.copy:
    src: "docker.json/{{ ansible_facts['hostname'] }}"
    dest: /etc/docker/daemon.json
    owner: root
    group: root
    mode: "0644"
    force: true
    validate: dockerd --validate --config-file %s
  notify: DockerRestart
...
