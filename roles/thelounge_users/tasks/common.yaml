---
- name: Add users
  community.docker.docker_container_exec:
    container: thelounge
    argv:
      - thelounge
      - add
      - "{{ item.user }}"
      - "--password"
      - "{{ item.password }}"
  loop: "{{ thelounge_users }}"
...
