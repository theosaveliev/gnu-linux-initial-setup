#!/usr/bin/bash

{% if thelounge_name is defined %}
curl https://{{ tta_control_domain }}/notify_thelounge_users -H "Content-Type: application/json" -d '{"token": "{{ tta_control_token }}"}'
{% endif %}

{% if filebrowser_name is defined %}
curl https://{{ tta_control_domain }}/notify_filebrowser_users -H "Content-Type: application/json" -d '{"token": "{{ tta_control_token }}"}'
{% endif %}
