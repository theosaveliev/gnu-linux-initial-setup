---
- name: Template systemd service
  ansible.builtin.template:
    src: telegram.bot.service.j2
    dest: "/etc/systemd/system/{{ service.name }}.service"
    owner: root
    group: root
    mode: "0644"
    force: true

- name: Start telegram_bot
  ansible.builtin.systemd_service:
    name: "{{ service.name }}.service"
    state: restarted
    enabled: true
    masked: false
    daemon_reload: true

- name: Wait for telegram_bot to be ready
  ansible.builtin.wait_for:
    host: "127.0.0.1"
    port: "{{ service.port }}"
    timeout: 300
    state: started
...
