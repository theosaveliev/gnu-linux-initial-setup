---
- name: Replace users dir
  ansible.posix.synchronize:
    src: "thelounge/{{ service.name }}/users"
    dest: "{{ ansible_facts['user_dir'] }}/{{ service.name }}/config/users"
    archive: true
    recursive: true
    delete: true
    mode: "push"
    rsync_opts:
      - "--include=*.json"
      - "--exclude=*"

- name: Update file ownership
  ansible.builtin.file:
    path: "{{ ansible_facts['user_dir'] }}/{{ service.name }}/config/users"
    owner: "{{ ansible_user }}"
    state: directory
    recurse: true

- name: Update directory permissions
  ansible.builtin.file:
    path: "{{ ansible_facts['user_dir'] }}/{{ service.name }}/config/users"
    owner: "{{ ansible_user }}"
    state: directory
    mode: "0700"
...
